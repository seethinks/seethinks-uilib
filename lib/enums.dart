// 对齐方式
enum FormLayout { horizontal, vertical }

enum InputType { text, password, phone, card }

// button类型
enum ButtonType { primary, gost }

enum ToastType { NOTHING, LOADING, SUCCESS, FAIL, INFO }